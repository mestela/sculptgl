import './hammer.js';

const Hammer = window.Hammer;
const { Manager, Pan, Pinch, Tap, Press, Rotate, Swipe } = Hammer;

export { Manager, Pan, Pinch, Tap, Press, Rotate, Swipe };
export default Hammer;
